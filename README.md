# 🧠 Obsidian Toolkit — Набор утилит для управления персональной базой знаний

Этот репозиторий содержит **четыре независимых Python-модуля**, созданных для автоматизации работы с [Obsidian](https://obsidian.md) — системой пер персонального знания, дневника и самонаблюдения.

Каждый модуль решает конкретную задачу:
- 📲 **Синхронизация с телефоном**
- 📅 **Управление ежедневными заметками**
- 🧠 **Интервальное повторение (Spaced Repetition)**
- 📊 **Сбор и анализ личной статистики**

Вы можете использовать **все модули сразу** или **только нужные** — они не зависят друг от друга.

---

## 📁 Структура проекта

```
scripts/
├── files/    → Массовое переименование и форматирование файлов
├── srs/      → Интервальное повторение по тегу #повторить
├── stats/    → Сбор и анализ данных из заметок (вес, настроение, привычки)
└── sync/     → Двусторонняя синхронизация с телефоном через SFTP
```

---

## 🚀 Быстрый старт

1. **Клонируйте репозиторий**
   ```bash
   git clone https://github.com/MrTimofeev/obsidian-labs.git
   cd obsidian-toolkit
   ```

2. **Установите зависимости** (только для `sync/`, остальные — без внешних пакетов):
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте пути и параметры**  
   Каждый модуль содержит **примеры конфигурации прямо в коде** (или через `config.json`).  
   → Просто откройте нужный скрипт и укажите путь к своей базе Obsidian.

4. **Запускайте нужный модуль**
   ```bash
   # Примеры:
   python scripts/files/rename_format_file_script.py
   python scripts/srs/spaced_repetition_script.py
   python scripts/stats/all_stats_in_json.py
   python scripts/sync/obsidian_sync.py
   ```

---

## 📌 Краткое описание модулей

| Модуль | Назначение | Ключевая особенность |
|-------|-----------|----------------------|
| [`files/`](scripts/files/) | Приведение имён ежедневных заметок к формату `ГГГГ-ММ-ДД.md` | Поддержка рекурсивного переименования |
| [`srs/`](scripts/srs/) | Повторение важных заметок по методу SM-2 | Работает **только с тегом `#повторить`** |
| [`stats/`](scripts/stats/) | Извлечение и агрегация данных (вес, настроение, привычки) | Поддержка кастомных правил анализа |
| [`sync/`](scripts/sync/) | Синхронизация с Android-телефоном через Termux + SSH | Инкрементальная и полная синхронизация |

---

## ⚠️ Важно

- Все скрипты **работают с файловой системой** — сделайте резервную копию базы перед первым запуском.
- Файлы `.json` (статистика, повторения, конфиги) **не коммитятся** (см. `.gitignore`) — они создаются автоматически.
- Модули **не требуют веб-сервера, базы данных или облачных сервисов** — всё работает локально.

---

## 📄 Лицензия

Проект создан для личного использования. Распространяется «как есть», **без гарантий**. Используйте на свой страх и риск.

---

> 💬 **Совет**: начните с одного модуля — например, `files/`, чтобы упорядочить имена файлов. Затем подключите `srs/` для важных идей и `stats/` для самонаблюдения.  
> А `sync/` даст вам доступ к базе **везде и всегда**.

✨ **Ваша база знаний — ваш второй мозг. Автоматизируйте его!**

```
> 💬 Я не даю гарантию что все будет работать "из коробки" даже если вы выполните все шаги, данные скрипты были разработаны для личного пользования, возможно когда-нибудь я сделаю их более дружелюбными. 
