```markdown
# 📁 Files — Работа с именами и форматами файлов

Этот модуль содержит утилиты для массовой обработки файлов в вашей базе Obsidian, особенно — для **приведения имён ежедневных заметок к единому формату**.

> Скрипт создавался из-за проблемы с изначальным вариантом нейминга ежедневных файлов, так как они были несовместимы с плагином calendar  

## 🛠️ Скрипты

### `rename_format_file_script.py`

**Назначение**:  
Автоматически переименовывает файлы ежедневных заметок из формата:  
`D.M.YYYY.md` → `YYYY-MM-DD.md`

**Пример**:  
- Было: `5.4.2025.md`  
- Стало: `2025-04-05.md`

**Зачем это нужно?**
- Улучшает сортировку файлов в проводнике и в Obsidian.
- Обеспечивает совместимость с плагинами (например, **Daily Notes**, **Calendar**).
- Стандартизирует формат для других скриптов (например, статистики или SRS).

## ▶️ Как использовать

1. **Настройте путь** к папке с заметками в скрипте:
   ```python
   notes_folder = "D:/База Знаний/Daily"
   ```
   > 💡 Рекомендуется использовать **прямые слэши** (`/`) даже на Windows.

2. **Запустите скрипт**:
   ```bash
   python rename_format_file_script.py
   ```

3. Скрипт:
   - Рекурсивно обходит указанную папку (если `recursive=True`).
   - Находит файлы, соответствующие шаблону `число.число.число.md`.
   - Безопасно переименовывает их в формат `ГГГГ-ММ-ДД.md`.
   - Выводит отчёт: сколько файлов переименовано, пропущено или вызвало ошибку.

## ⚠️ Важно

- Скрипт **не перезаписывает** существующие файлы. Если `2025-04-05.md` уже существует — файл будет пропущен.
- Рекомендуется **сделать резервную копию** перед массовым переименованием.
- Скрипт работает **только с существующими файлами** — новые заметки нужно создавать уже в правильном формате (или использовать шаблоны Obsidian).

---
