# 📊 Stats — Сбор и анализ персональной статистики из заметок Obsidian

Этот модуль позволяет **автоматически извлекать количественные данные** из ваших ежедневных или тематических заметок и **анализировать их по заданным правилам**.

Идеально подходит для тех, кто ведёт **дневник самонаблюдения**, **трекер привычек**, **журнал настроения**, **веса**, **продуктивности** и т.п.

---

## 🧩 Как это работает?

### 1. **Сбор данных** (`all_stats_in_json.py`)
Скрипт сканирует указанную директорию и ищет в `.md`-файлах строки вида:
```markdown
вес:: 75
настроение:: 8
медитация:: да
количество задач:: 12
```

> 🔍 Формат: `ключ:: значение` 
> ⚠️ **Важно** ключ и значение разделены двойным двоеточием (при желании можно использовать плагин Dataview)

Все найденные пары сохраняются в файл **`stats.json`**, где каждая запись привязана к **дате файла** (имя файла должно быть в формате `YYYY-MM-DD.md` или содержать дату в содержимом).

Пример `stats.json`:
```json
[
  {
    "date": "2025-04-01",
    "вес": "75",
    "настроение": "8",
    "медитация": "да"
  },
  {
    "date": "2025-04-02",
    "вес": "74.8",
    "настроение": "6",
    "количество задач": "10"
  }
]
```

---

### 2. **Анализ данных** (`analyze_stats_in_json.py`)
Скрипт читает `stats.json` и агрегирует данные по правилам из файла **`analyz_config.json`**.

Поддерживаемые типы агрегации:
- `avg` — среднее
- `sum` — сумма
- `count` — количество записей
- `count_yes` — сколько раз встречалось «да» (для бинарных полей)
- `progress_to_goal` — процент выполнения цели (например, «выполнить 20 задач»)

Пример конфига (`analyz_config.json`):
```json
[
  {
    "field": "вес",
    "type": "numeric",
    "agg": ["avg", "min", "max"]
  },
  {
    "field": "медитация",
    "type": "binary",
    "agg": "count_yes"
  },
  {
    "field": "количество задач",
    "type": "numeric",
    "agg": {
      "type": "progress_to_goal",
      "goal": 20
    }
  }
]
```

Результат сохраняется в **`analayz_stats.json`** — готовый к визуализации или импорту в дашборд.

---

### 3. **Дополнительно: выборка по тегу** (`get_tag_data.py`)
Скрипт извлекает **весь текст**, находящийся под определённым тегом (например, `#мысль`), из всех заметок.

Полезно для:
- сбора всех цитат (`#цитата`)
- экспорта рефлексий (`#осознание`)
- анализа эмоционального тона мыслей

Результат — список текстовых фрагментов, сохраняется в `tagged_data.json` (или выводится в консоль).

---

## ▶️ Как использовать

### Шаг 1: Подготовьте заметки
Убедитесь, что ваши файлы содержат данные в формате:
```markdown
# 2025-04-01

вес:: 75  
настроение:: 8  
спорт:: да  
задачи:: 15

#мысль
Сегодня понял, что регулярность важнее интенсивности.
```

> 💡 Имя файла должно быть `2025-04-01.md` — дата берётся из него.

### Шаг 2: Настройте пути
Откройте скрипты и укажите путь к вашей базе:
```python
# all_stats_in_json.py
DIRECTORY = ""
```

### Шаг 3: Запустите сбор
```bash
python all_stats_in_json.py    # → создаёт stats.json
python get_tag_data.py         # → выводит мысли с #мысль
```

### Шаг 4: Настройте анализ
Отредактируйте `analyz_config.json` под свои метрики.

### Шаг 5: Запустите анализ
```bash
python analyze_stats_in_json.py  # → создаёт analayz_stats.json
```

---

## 📁 Файлы проекта

| Файл | Назначение |
|------|-----------|
| `all_stats_in_json.py` | Сбор структурированных данных (`ключ: значение`) |
| `analyze_stats_in_json.py` | Агрегация и анализ по конфигурации |
| `get_tag_data.py` | Извлечение текста под тегом (например, `#мысль`) |
| `stats.json` | Сырые данные (вывод `all_stats_in_json.py`) |
| `analyz_config.json` | Правила анализа (пример — в комментариях кода) |
| `analayz_stats.json` | Результат анализа |

> ⚠️ **Важно**: файлы `.json` не включены в репозиторий (см. `.gitignore`). Используйте их локально.

---

## 💡 Идеи для использования

- Построение графиков в **Obsidian** через плагин **Chart** или **QuickAdd + Dataview**
- Еженедельный отчёт: «Среднее настроение — 7.2, медитация — 5 дней из 7»
- Отслеживание прогресса: «Выполняю 78% от цели в 20 задач»
- Экспорт мыслей за месяц для рефлексии

---

## 🔜 Планы

- [ ] Поддержка YAML-фронтматтера
- [ ] Автоматическое определение даты из содержимого (если нет в имени файла)
- [ ] Генерация отчёта в Markdown для Obsidian
- [ ] Визуализация через встроенный веб-сервер (Plotly/Dash)

---

> 🌱 Этот модуль превращает вашу базу знаний в **персональную систему мета-анализа** — вы не просто пишете, а **видите динамику своей жизни**.
