# 🧠 Spaced Repetition (SRS) для Obsidian

Этот модуль реализует **систему интервального повторения (Spaced Repetition)** на основе адаптированного алгоритма **SM-2**, специально для работы с заметками в Obsidian.

📌 **Ключевое условие**: в заметку **должен быть добавлен тег `#повторить`** — только такие файлы попадают в систему повторения.

Цель — помочь вам **актуализировать важные знания**, автоматически напоминая о них в оптимальные моменты, и давая возможность обновлять содержимое при устаревании.

---

## 🔄 Как это работает?

1. **Сканирование базы**  
   Скрипт рекурсивно ищет все `.md`-файлы в указанной директории, **содержащие строку `#повторить`**.

2. **Регистрация новых заметок**  
   Найденные файлы, ещё не включённые в систему, добавляются в файл `reviews.json` с датой первого повторения — **сегодня**.

3. **Формирование списка на сегодня**  
   Скрипт выбирает все заметки, у которых `date_for_repeat ≤ сегодня`.  
   ➕ **Если таких нет** — автоматически выбирает **5 случайных** из уже зарегистрированных (чтобы не прерывать практику).

4. **Создание файла повторения**  
   Генерируется файл:  
   `Повторение.md`  
   с кликабельными ссылками вида `[[имя_файла.md]]`, количеством повторений и датой следующего напоминания.

5. **Оценка и обновление**  
   После прочтения вы возвращаетесь в терминал и вводите оценки по шкале:
   - `0` — не помню
   - `1` — трудно вспомнить
   - `2` — легко вспомнил

   На основе оценки пересчитывается дата следующего повторения.

---

## 🗂️ Формат данных (`reviews.json`)

```json
[
  {
    "file_name": "Важные мысли.md",
    "relative_path": "Мои заметки/Важные мысли.md",
    "date_for_repeat": "2025-04-10",
    "number_of_repetitions": 2,
    "is_random": false
  }
]
```

> 💡 Файл `reviews.json` хранит **историю повторений** и **план** — не удаляйте его!

---

## ▶️ Как использовать

1. **Добавьте тег `#повторить`** в любую заметку, которую хотите помнить надолго:
   ```markdown
   # Моя идея
   
   Это важная мысль, которую я не хочу забыть.
   
   #повторить
   ```

2. **Настройте пути** в `SRSManager` (в начале скрипта):
   ```python
   self.base_dir = "D:/База Знаний/pc/Programming"
   self.reviews_file = ".../08. Повторение/reviews.json"
   self.review_list_file = ".../08. Повторение/Повторение.md"
   ```

3. **Запустите скрипт**:
   ```bash
   python spaced_repetition_script.py
   ```

4. **Следуйте инструкциям**:
   - Откройте сгенерированный `Повторение.md`
   - Прочитайте заметки
   - Вернитесь в терминал и введите оценки, например:  
     `Важные мысли.md:2, Идея.md:1`

---

## 📏 Алгоритм повторения

| Оценка | Действие |
|--------|--------|
| `0` | Полный сброс: интервал = 0 дней (повторить завтра) |
| `1` | Трудно: → **1 день** (если 1-й повтор) или **6 дней** (если 2-й) |
| `2` | Легко: → предыдущий интервал × **2.5** |

> Алгоритм вдохновлён SM-2 (как в Anki), но упрощён для личного использования.

---

## ⚠️ Важно

- Скрипт **игнорирует папку `05. Шаблоны`** (чтобы не ловить шаблоны с `#повторить`).
- Поддерживаются **вложенные директории**.
- **Тег чувствителен к регистру**: используйте именно `#повторить` (в нижнем регистре).
- Если не ввести оценки — даты **не обновятся**, но файл повторения будет создан.

---

## 🔜 Возможные улучшения

- [ ] Поддержка кастомного тега через конфиг (`#srs`, `#flashcard` и т.д.)
- [ ] Автоматическое создание папки `08. Повторение`, если её нет
- [ ] Возможность указывать путь через аргумент командной строки
- [ ] Интеграция с Obsidian URI для быстрого открытия файла

---

> 💬 **Совет**: добавляйте `#повторить` в заметки, содержащие **ключевые идеи, решения задач, цитаты или принципы**, которые хотите удержать в долгой памяти.  
> Запускайте скрипт ежедневно — и ваша база знаний станет «живой» системой обучения!
