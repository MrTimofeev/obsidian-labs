# 📲 Sync — Синхронизация базы Obsidian с телефоном

Этот модуль позволяет **двусторонне синхронизировать** вашу локальную базу Obsidian с удалённым устройством (например, **Android-телефоном с Termux + SSH-сервером**) по протоколу **SFTP**.

Поддерживается **инкрементальная синхронизация**: копируются только новые и изменённые файлы, а также корректно обрабатываются удаления.

---

## 🔄 Два режима синхронизации

### 1. `obsidian_sync.py` — **Инкрементальная синхронизация**
- Сравнивает **время последнего изменения** файлов на локальной машине и на удалённом устройстве.
- Копирует только то, что изменилось.
- Удаляет файлы на удалённой стороне, если они удалены локально (и наоборот — при двустороннем режиме).
- **Безопасна для частого запуска** (например, по cron каждые 10 минут).

> ⚠️ **Важно**: Я отказался от этого варианта т.к не делаю заметок на телефоне, не даю гарантии что будет работать так как заявлено, используйте на свой страх и риск (рекомендую сделать резервную копию заметок перед использованием)

### 2. `obsidian_sync_v2.py` — **Полная загрузка (one-way)**
- **Полностью выгружает** указанную локальную директорию на удалённое устройство.
- Удаляет **всё содержимое** удалённой папки перед загрузкой.
- Подходит для **инициализации** или **полного восстановления** базы на телефоне.
- ✋ **Используйте с осторожностью** — это односторонняя операция (локальное → удалённое).

---

## ⚙️ Требования

### На компьютере:
- Python 3.8+
- Пакет `paramiko` (устанавливается через `requirements.txt`)

### На телефоне (Android):
1. Установите **Termux** из F-Droid
2. В Termux выполните:
   ```bash
   pkg install openssh
   ssh-keygen -t ed25519
   echo "sshd: ALL" >> $PREFIX/etc/ssh/sshd_config
   sshd
   ```
3. (Опционально) Настройте автозапуск `sshd` через Termux:Widget или Tasker.

> 🔐 **Безопасность**: рекомендуется использовать **авторизацию по SSH-ключу**, а не по паролю.

---

## 🔧 Настройка

1. **Создайте файл `config.json`** в корне проекта (рядом с `sync/`):

   ```json
   {
     "source_dir": "ваш путь до базы",
     "remote_dir": "/data/data/com.termux/files/home/storage/shared/Obsidian",
     "ssh": {
       "hostname": "192.168.1.100",
       "port": 8022,
       "username": "u0_a123",
       "password": "ваш_пароль",       // или "key_filename": "путь/к/id_ed25519"
       "timeout": 10
     }
   }
   ```

   > 💡 Если используете **ключ**, укажите:
   > ```json
   > "key_filename": "C:/keys/id_ed25519"
   > ```
   > и **удалите строку с `password`**.

2. Убедитесь, что:
   - `source_dir` — путь к вашей локальной базе Obsidian.
   - `remote_dir` — путь на телефоне (в Termux, папка должна существовать).

---

## ▶️ Запуск

```bash
# Инкрементальная синхронизация (рекомендуется)
python sync/obsidian_sync.py

# Полная перезапись (осторожно!)
python sync/obsidian_sync_v2.py
```

Скрипт:
- Установит SSH-соединение
- Откроет SFTP-сессию
- Выполнит синхронизацию
- Выведет отчёт: сколько файлов загружено, удалено, пропущено

---

## ⚠️ Важно

- **Резервное копирование**: перед первым запуском `obsidian_sync_v2.py` сделайте бэкап на телефоне!
- **Имена файлов**: избегайте кириллицы и спецсимволов — могут возникнуть проблемы с кодировкой на Android.
- **Размер базы**: если у вас >10 ГБ — синхронизация может занять время и потребовать много трафика.
- **Сетевое подключение**: телефон и компьютер должны быть в **одной Wi-Fi сети**.

---

## 💡 Советы

- Используйте **статический IP** для телефона в роутере (чтобы `hostname` не менялся).
- Автоматизируйте синхронизацию через **Task Scheduler (Windows)** или **cron (Linux/macOS)**.
- Для безопасности — **отключайте SSH-сервер** в Termux, когда не используется.

---

> 📲 Теперь ваша база знаний всегда с вами — на компьютере и в кармане!
```